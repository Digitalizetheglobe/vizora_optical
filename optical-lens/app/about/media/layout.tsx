import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Media & Press', description: 'Vizora Optics media resources, press releases and brand assets for journalists.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Our History', description: 'The story and journey behind Vizora Optics — from inception to a trusted premium ophthalmic lens brand.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

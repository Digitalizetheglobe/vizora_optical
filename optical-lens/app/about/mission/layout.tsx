import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Mission & Values', description: 'The mission, vision and core values that guide every decision at Vizora Optics.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

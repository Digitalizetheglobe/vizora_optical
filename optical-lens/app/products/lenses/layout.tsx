import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Optical Lenses — Progressive Series', description: 'Explore the full Vizora progressive lens range: V SELECT HD, V FLOW HD, V ELITE HD, and V INFINITY 4K.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

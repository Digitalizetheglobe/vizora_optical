import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Compare Products', description: 'Compare all Vizora Optics progressive lenses and coatings side-by-side to find the right solution for every patient.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

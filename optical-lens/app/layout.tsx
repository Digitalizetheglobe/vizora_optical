import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import Navbar from "./components/Navbar";
import Footer from "./components/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  weight: ["400", "600", "700"],
});

export const metadata: Metadata = {
  title: {
    template: "%s | Vizora Optics",
    default: "Vizora Optics - Premium Ophthalmic Lenses",
  },
  description:
    "Vizora Optics delivers precision-engineered ophthalmic lenses for eye care professionals. Premium progressive and single vision lens solutions with advanced coating technologies.",
  keywords: ["ophthalmic lenses", "progressive lenses", "optical lenses", "Vizora Optics", "eye care"],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
